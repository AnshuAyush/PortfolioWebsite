import { useState } from 'react'
import { ChevronDown, ChevronRight, GitBranch } from 'lucide-react'
const tree = [{ label:'Spring Ecosystem', children:['Spring Boot','Spring MVC','Spring Security','Spring Cloud','Spring Data JPA','Spring Kafka','Spring Boot Actuator','Hibernate','JDBC','Maven'] }, { label:'Frontend & APIs', children:['React','REST APIs','GraphQL','OAuth2','JWT'] }, { label:'Distributed Systems', children:['Microservices','Kafka','Event-driven Architecture','Distributed Systems','Message Queues'] }, { label:'System Design & Architecture', children:['Domain-Driven Design (DDD)','Design Patterns','High Availability (HA)','Fault Tolerance','Horizontal Scalability','Distributed Caching','API Gateway','Service-Oriented Architecture (SOA)','Data Mesh','Circuit Breaker Pattern'] }]
export default function TrieVisualization() { const [expanded, setExpanded] = useState({ 'Spring Ecosystem':true, 'Frontend & APIs':true, 'Distributed Systems':true, 'System Design & Architecture':true }); return <div className="trie-card glass"><div className="trie-row"><GitBranch size={16} color="#57d9b9"/><span className="trie-main root">root / engineering</span></div>{tree.map((item) => <div key={item.label}><div className="trie-row"><button className="trie-toggle" onClick={() => setExpanded({...expanded, [item.label]: !expanded[item.label]})}>{item.children.length ? (expanded[item.label] ? <ChevronDown size={15}/> : <ChevronRight size={15}/>) : <span style={{width:15}}/>}</button><span className="trie-main">{item.label}</span></div>{expanded[item.label] && <div className="trie-level">{item.children.map((child) => <div className="trie-row" key={child}><span className="trie-leaf">└─ {child}</span></div>)}</div>}</div>)}</div> }
